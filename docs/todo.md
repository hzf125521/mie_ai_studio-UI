1. 首页的Algorithm List怎么获得？
2. step2中
2.1 实际获取特征序列的接口由5865提供还是mie8840提供（也是要依赖5865的）？
不论由谁实现，都是根据设备onlycode、时间范围、特征列表、工况列表，再附加上一些条件（例如：运行的数据）来获取多维序列。
2.2 先筛选展示，再添加多维数据集样本。
要对信号的时间范围（或者样本时间点数）进行限制，不能太多了。（考虑做一个设置页面，对关键参数进行配置管理？）
从数据集样本获取实际序列（即点击样本列表中的样本），可以复用2.1中的接口，也可以在5865端直接提供一个根据样本id获取序列的接口。
2.3 在此之前，要根据设备的onlycode，获取该设备的所有特征和工况，给前端筛选使用。

2.4 在“数据集”数据结构中，要不要单独增加“工况”字段？常规特征的时间间隔和工况的时间间隔一样吗？—余伟、毛？

2.5 
不需要data_path字段，因为实际的序列是根据onlycode、时间范围、特征列表、工况列表来请求获取的。不过也不一定，可能实际5865后端就是这种指针关系，需要确认。—蒋？

2.6 PCA是延迟计算的，即再获取到多维序列后，才进行PCA计算。PCA计算放在后端还是前端？


3. step3中
3.1 首先，从5865端获取数据集样本信息，然后从mie8840端要获取预处理配置、模型及其参数。

3.2 在勾选数据集样本的时候，在前端就要检查多样本的特征是否一致。当然后端在训练前还要再检查一次。

3.3 实际训练的接口由mie8840提供。具体为：将5865提供的数据筛选接口和mie-studio提供的底层训练接口进行封装。
根据"train_dataset_ids": ["sig-1", "sig-2"]，从5865端获取这两个数据集样本的多维序列（见2.2）。
然后根据预处理配置进行预处理，最后调用mie-studio提供的训练接口进行训练。


3.4 在mie-studio开始训练时，将status字段设置为"training"。
训练完成后，通过修改接口，将status字段设置为"trained"，并记录下训练指标（例如：损失、准确率）。并记录模型uuid。训练时间?

3.5 前端通过轮训获取模型训练状态，直到训练完成。
模型数据结构中增加模型产物字段train_artifacts，记录score、阈值、中间pca（如果中间pca为2D或者3D，则记录pca结果，否则不记录，调用8840的接口重新计算。调用8840的接口重新计算时，是否可以前端缓存，减少重复调用？）等。
轮训哪个接口？“获取模型列表”？
“获取模型列表”和“获取模型详情”应该可以合并。——最终决定不合并：按需加载、只轮询“列表”接口，请求非常轻量；如果接口不拆分，前端每隔 5 秒就要把所有模型的全量详情数据重新拉取一遍，这是不可接受的。前端在“获取模型列表”后，如果有training状态的模型，就轮询“获取模型列表”接口刷新状态，且默认使用“获取模型详情”展示第一个trained的模型的详情信息（没有trained的话则不展示详情）。

3.6模型数据结构中，model_file_url应该没有必要，因为模型都统一存储在一个位置，只需要记录模型文件的文件名即可（文件名要确保唯一？）。


以下为验证阶段：
3.7 
验证数据结构中增加验证产物字段validation_artifacts，记录score、阈值、中间pca（如果中间pca为2D或者3D，则记录pca结果，否则不记录，调用8840的接口重新计算。调用8840的接口重新计算时，是否可以前端缓存，减少重复调用？）等。
轮训哪个接口？“获取验证列表”？



3. 数据集样本、模型的删除是直接从数据库中删除吗？还是标记为删除？—蒋
训练阶段和验证阶段，要设计避免重复点击的校验。
增加操作用户的信息记录。
对信号样本、模型数量、验证数量进行限制。